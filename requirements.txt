# Thermal Report - Python Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
numpy>=1.19.0
pillow>=8.0.0
python-dotenv>=1.2.1

# For FLIR image extraction
# Uses opencv-python-headless to avoid libGL/X11 dependencies in containers
flirimageextractor==1.2.11
opencv-python-headless>=4.5.0

# For visualisation (optional but recommended)
matplotlib>=3.3.0

# Web interface
flask>=2.0.0

# WSGI server for production (Cloud Run)
gunicorn>=21.0.0
google-auth==2.29.0
google-auth-httplib2==0.2.0
google-api-python-client==2.126.0
