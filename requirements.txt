# Thermal Report - Python Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
numpy>=1.19.0
pillow>=8.0.0
python-dotenv>=1.2.1
gunicorn
google-api-python-client
google-auth
google-auth-httplib2
google-auth-oauthlib

# For FLIR image extraction
flirimageextractor>=1.0.0

# For visualization (optional but recommended)
matplotlib>=3.3.0

# For web interface
flask>=2.0.0

# HTTP (recommendations/shared-notes fetching)
requests>=2.31.0

# PDF generation (preferred)
weasyprint>=60.0

# Alternative PDF backends (optional)
# pdfkit>=1.0.0          # Requires wkhtmltopdf system package
# xhtml2pdf>=0.2.15
